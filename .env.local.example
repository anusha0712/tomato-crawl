# Server-side only. This key must NEVER get a NEXT_PUBLIC_ prefix — that would
# ship it to the browser. It is read in a route handler and stays on the server.
# Restricted to Routes API in the Google Cloud console.
GOOGLE_MAPS_API_KEY=paste-your-AIza-key-here
